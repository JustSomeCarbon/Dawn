space::Main;

func main int ()
{
	a int = 2;
	b int = 3;

	c bool = true;
	d bool = false;
	
	// test mathematical operations and their output
	IO::out(a + b);
	IO::out(b - a);
	IO::out(a * b);
	IO::out(b / a);
	IO::out(b % a);

	// test value comparison operations
	IO::out(a == a);
	IO::out(a == b);
	IO::out(a > b);
	IO::out(a < b);
	IO::out(a >= b);
	IO::out(a <= b);
	IO::out(b != b);
	IO::out(b != a);

	IO::out(c && c);
	IO::out(c && d);
	IO::out(c || c);
	IO::out(c || d);
	IO::out(d || d);
	IO::out(!c);
	IO::out(!d);

	0;
}
