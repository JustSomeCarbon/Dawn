space::Main;

fn main int ()
{
	a char = 'a';
	b char = 'b';

	// contatenation of characters results in a string
	c string = a | b;
	IO::out(a);
	IO::out(b);
	IO::out(c);
	IO::out(a == a);
	IO::out(a == b);

	0;
}
