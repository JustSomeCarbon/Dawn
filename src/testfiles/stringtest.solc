space::Main;

// test defining and using strings
fn main int ()
{
	a string = "hello, ";
	b string = "world!";

	// concatenate the variables a and b (a before b)
	c string = a | b;
	IO::out(a);
	IO::out(b);
	IO::out(c);

	0;
}
