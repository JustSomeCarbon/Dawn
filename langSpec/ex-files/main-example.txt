module.Main

// example of importing separate modules
use Math

fn main int ()
{
    // variables are statically typed
    foo int = 1;
    bar char = 'a';

    // define multiple variables on single line
    a, b float = 0.0, 1.0;
}

// define a function
// returns are implicit for the last statement of a block
fn square int (x int)
{
   return x * x;
}
